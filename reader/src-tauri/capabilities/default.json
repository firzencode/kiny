{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "reader 默认权限：读库目录、文件对话框、打开日志目录。.kip 导入由 Rust 侧 File::open 流式读，webview 不持有全盘文件读权限",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "log:default",
    "opener:default",
    "clipboard-manager:allow-write-text",
    { "identifier": "opener:allow-open-path", "allow": [{ "path": "$APPLOG" }, { "path": "$APPLOG/**" }] },
    { "identifier": "fs:allow-read-text-file", "allow": [{ "path": "$APPDATA/**" }, { "path": "$APPLOG/**" }] },
    { "identifier": "fs:allow-read-dir", "allow": [{ "path": "$APPDATA/**" }] },
    { "identifier": "fs:allow-exists", "allow": [{ "path": "$APPDATA/**" }] }
  ]
}

# Changelog

本项目所有值得记录的变更收录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [0.13.0] - 2026-08-14
### Added

- 角色台词着色：作品里声明角色后，在台词行开头标出说话人（`<名字> 台词` 或 `名字：台词`），阅读时该行按角色显示不同颜色，读者一眼分得清谁在说话。颜色默认自动分配、随作品主题保持可读，也可自己指定；编辑器里点「文件 → 作品角色…」即可增删角色、调整配色。
- 正文分割线 `@divider()`：在正文里插一条分隔线，用于场景转换与幕间。默认是一条居中短线、随作品主题走；传一个类名（`@divider("幕间")`）即可用作品 css 改成任意样式。
- 命令行工具新增 `--raw <字段>`，直接输出文件原文而非 JSON，可配合重定向把内容存成文件。
- 编辑器预览新增「快进」开关：开着时正文瞬间显示、停顿与逐行等待一律不等，点完一个选项直接到下一个决定点，调试剧情分支时快得多。只影响预览，不改作品本身，也不影响读者。

### Changed

- 命令行单次写入的内容上限放宽到 16 MiB（约 560 万汉字）。

### Fixed

- 页面底色终于所见即所设：作品没有设置背景图时，阅读页的底色就是主题里写的那个颜色，不再被一层压暗的遮罩罩住而发灰（浅色主题上尤其明显）。有背景图时，编辑器预览与实际阅读页的明暗也统一成了同一档。
- 在 JS 里拼出的多行文本插进正文后，打字揭示完成时不再并成一排——文本里的换行与 `<br>` 等价，正文、选项、固定区域三处一致；用空格对齐的排版也不再在揭示结束的瞬间跳变。
- 命令行写入超大内容时，提示明确说明内容超过上限，不再报出与实际原因无关的错误。
- 命令行连不上编辑器时区分原因：口令不匹配就说口令不匹配，不再一律提示重开外部控制。

## [0.12.0] - 2026-08-10
### Added

- 作品主题：新建项目自带一个 `theme.css`，点开它就是一个可视化的外观编辑器——配色、字体、字号行高、阅读栏宽直接拖着调，改完预览立刻换肤；想手写 CSS 就切「原文」页，同一个文件两个视图。调整只改动对应的那几个值，你写的注释与自定义样式原样保留。
- 内置四套主题（石墨夜 / 暖纸 / 午夜蓝 / 素白），一键换整体风格。
- 编辑器里现在建得出 `.css` 等文本文件了：新建文件时带已知扩展名就按该类型创建，不再被一律当成故事文件。
- 资源管理器里点开图片或音频即可在编辑区查看：图片默认适应窗口、可切 1:1 查看并显示像素尺寸，音频可直接试听并显示时长。

### Changed

- 网页书库导入时为封面生成缩略图存入书库，大封面不再撑大浏览器存储、书架列表也更快；原图随故事包完整保留，阅读时照常使用。
- AI 助手现在知道自己能读写作品外观文件（`theme.css` 等作品 CSS），预览走查剧情时也能退回上一步而不必从头重放。

### Fixed

- 作品 CSS 现在只作用于预览区，写 `body` / `*` 这类通用选择器不会再改到编辑器界面；导出网页、网页书库与阅读器里的表现不变。
- 项目里有 `theme.css` 等非故事文件时，AI 助手不再报出并不存在的语法错误。

## [0.11.0] - 2026-08-06
### Added

- 正文插图：`@img("assets/图片", "替代文字")` 在正文流里插一张图，随正文滚动、留在阅读历史里。
- 句中停顿新增定时档 `<pause=毫秒>`：停满指定时长后自动续显后半句，等待期间点击无效。
- 网页书库在浏览器不允许本地存储时（隐私模式 / 站点数据被禁 / 老浏览器）不再整体不可用，改为「一次性导入即读」的临时模式：可正常阅读，但书与存档不留存。

### Changed

- 网页书库的阅读存档改存 IndexedDB，不再受 localStorage 5MB 配额限制，长篇多存档也放得下；旧版本存下的存档在升级后自动迁移，无需手动处理。

### Fixed

- editor 在光标所在行拖选文字时，选中高亮不再被当前行底色盖住而看不见；暗色「石板墨」与「素雪白」的选中色相应加深一档，落在当前行上也能一眼看清。

## [0.10.0] - 2026-07-28
### Added

- 作品可自带前端资源：项目内任意位置的 `.css` 自动加载、字体文件自动注册（族名 = 文件名去扩展名），正文用 `<font=名>` 换字体、`<class=名>` 挂语义类交给作品 css；编辑器可直接编辑这些文本资源、预览可切换「应用作品主题」，导出网页照样生效。
- 新增 `@sleep(毫秒)` 命令：在行与行、命令与命令之间插入定时停顿，用于场景切换 / 音画之间的演出空拍，读者点击不可跳过。
- 正文支持句中停顿 `<pause>`：前半句显示后停住，读者点击才续显后半句，同一行内制造悬念节奏。
- 新增 `@panel(槽位, 模板)` 固定区域：左 / 右侧栏、底栏、正文后栏四槽，模板里的 `{变量}` 变了自动刷新，适合 RPG 状态栏、章节指示；外观可用作品 css 自定义。
- Kin 脚本支持动态跳转：节点可存进变量（内置 `$nodes` 节点表），`-> {表达式}` 在运行时决定跳转目标，服务数据驱动的地图 / 返回点 / 配置表剧情；`"名字" in $nodes` 可测节点存在性。
- editor F1 语法参考新增「动态跳转」条目（每种用法一个最小示例）。

### Fixed

- shelf / reader 阅读顶栏长书名现省略截断，不再在窄屏把「存档 / 读档」按钮挤出屏幕。

## [0.9.0] - 2026-07-22
### Added

- editor 外观设置支持自定义主题：可新建 / 取色编辑 / 命名 / 导入导出命名主题，预设与自定义主题并存、即选即应用，语法高亮跟随所选主题明暗基底。
- editor 新增内置浅色主题「素雪白」（纯白文档区 + 中性灰阶 chrome）。
- editor 左侧新增「待办」面板：汇总项目脚本内 `// TODO` / `// FIXME` 为可跳转、按文件分组的列表，随编辑刷新。
- editor 预览新增「返回上一步」：撤销上一次选项 / `@input` 回到上一决定点重选，可连退到预览起点（作者调试用，不影响正式阅读）。
- 新增 shelf 网页书库阅读器：浏览器里导入 `.kip` 故事包即读，带持久书库（封面 / 续读入口 / 删除）与多档存读档；界面与交互与桌面 reader 同一套暗色主题（含手机端适配），作者可自托管部署到任意静态位置。

### Fixed

- engine 修复存读档三处正确性缺陷：restore 不再重放选项 / `@input` 的求值副作用（文本变体 / 随机 / 占位符读档后不再漂移）、preamble 声明的函数读档后可正常调用、读档错误按类型区分而非一律报「损坏」。
- engine 存读档现支持 `Map` / `Set` / `Date` 等非 JSON 全局值（白名单编解码），此前这类状态经存档后被丢弃。
- engine 修正若干求值 / 存档语义：宿主重复轮询「能否继续」不再重复触发副作用；插值结果为空且非粘连的行不再产生空行；快照指纹改用结构序号，随机与重放计数细节校准。

## [0.8.0] - 2026-07-19
### Added

- cli 终端播放器（`npm run play`）现支持 `@input` 输入框：停在输入点提示输入、提交后继续（此前含输入的故事被静默截断为结束）。
- 导出的独立网页 / 在线 demo 现自动记住阅读进度：刷新后重放恢复到上次位置；改动故事后旧进度安全失效，并提供「重新开始」清除存档。
- reader 桌面支持多文件拖放批量导入（此前只导入第一个、其余静默丢弃）。
- editor AI 设置页新增 API key 明文存储风险提示与「一键清除」按钮：明示 key 以明文存于本机、共享设备用完可一键清空。

### Changed

- reader 导入 .kip 改为原生流式读取，大文件内存占用显著降低；同时收窄应用文件访问权限（webview 不再持有全盘读权限）。
- reader 自动续读存档与 editor 长会话实时预览的性能 / 内存占用优化（存档索引与编译产物按需缓存、书架续读探测批量化）。

### Fixed

- engine 修复带参节点内用限定名跳转自己的子节点（`-> 本节点.子节点`）时参数 / 局部变量被清空导致的运行期崩溃；逻辑行运行错误现带文件定位，多文件项目可直接定位报错文件。
- engine 修复停留在选项 / 命令暂停点时被宿主反复轮询会误报「疑似死循环」崩溃的问题（长时间停在选项界面不再有无操作崩溃风险）。
- editor 修复保存期间继续输入的内容可能被误标为「已保存」而在退出时静默丢失的竞态；AI / 外部控制的命令参数现做运行时校验，畸形调用不再可能损坏文档内容。
- reader 导入 .kip 增加解压资源上限（防恶意构造的压缩包写满磁盘）；存档改为原子写入，应用崩溃 / 掉电不再可能损坏自动续读存档。
- 播放层修复两处打字机被瞬显跳过的问题：点选项后紧跟 `@clear` 的下一行、以及重开 / 读档后的第一行，现都正常逐字打出。
- 导出的独立网页加载失败时显示具体错误（不再永久卡「加载中……」或白屏）；导出数据损坏时给出「请重新导出」的明确提示。
- reader 导入 / 读档失败时透传具体诊断（如「不是合法的 zip / .kip」「入口文件不存在」），不再一律显示泛化的「导入失败」。
- editor 文件删除操作增加路径穿越防护，AI / 外部控制无法再删除项目目录之外的内容。
- editor 文件写入 / 改名接口补齐路径穿越防护，AI / 外部控制无法再写到项目目录之外。
- editor 切换 / 打开另一个项目时若有未保存改动会先弹确认，避免静默丢失；AI 运行期间切换项目不再可能把旧项目的 AI 输出写进新项目。
- editor AI 面板：agent 触及工具调用轮数上限时明确提示（不再静默停在半途）；退出 / 关闭项目前保存最后一轮对话不丢；供应商返回畸形响应时给可读报错而非崩溃。
- editor 校验补齐更多缺口：顶层开场里的坏跳转、带参子节点的误用、对内置函数名赋值等现在校验期即报错，不再运行期才崩。
- reader 手动存档失败时给真实「存档失败」提示（不再谎报已存档）；快速连点存档 / 选项不再乱序落盘。
- 播放器富文本在「打字机揭示 → 定格」切换的瞬间不再样式闪变（宿主定制的加粗 / 斜体等两阶段一致）。
- 崩溃取证对话框现完全键盘可达（打开移焦、Esc 关闭、Tab 循环回环）；全局错误处理器重复安装不再叠加监听。
- tutorial 损坏的本地进度不再导致空白章节 / 固化 "NaN"；cli 参数缺失或非法在本地拦截并给用法提示，不再发无效请求。
- editor 代码区选中文本的高亮对比度修复：选中背景改用中性 slate 并使其特指度压过 CodeMirror 绘制层默认样式，暗 / 亮主题下任意语法高亮 token 被选中时文字都清晰可读（此前选中背景与金色系 token 撞色、暗色下自定义选中色不生效）。
- reader（Android）修复热启动经「分享 / 打开 .kip」意图导入后，系统回收重建页面时书架出现重复条目的问题。

## [0.7.0] - 2026-07-12
### Added

- editor 新增「结构图」：剧情节点可视化视图（节点与跳转关系图，缩放 / 平移 / 点击节点跳转源码），与预览同区 tab 切换。
- editor 快捷键中央注册表 + 设置页可自定义：帮助屏改为 F1，Ctrl+/ 改为注释 / 取消注释当前行，菜单快捷键提示与真实绑定始终同步。
- 正文支持 galgame 式呈现节奏：`@step_mode("line")` 逐段点击推进（等待点击时正文下方有呼吸的推进提示三角），`@text_speed` / `@text_fade` 打字机逐字揭示 + 淡入（默认 80 字/秒、每字 300ms，打字中点击立即整段显示）；web-reader / reader / editor 预览统一生效，editor 预览在人工点选项 / 重开预览时也走打字机动画。
- Kin 新增 `@input` 读者输入命令：运行到此暂停故事、向读者请求一段文本并写入变量，供后续正文插值 `{变量}` 与条件判断使用（如让读者给主角起名，留空则保留默认）；reader / web-reader 播放时出现输入框。
- editor 启动页与主编辑器改为两个真正独立的 OS 窗口：启动窗按屏幕分辨率定尺寸、不可最大化 / 调整尺寸，打开项目后开编辑窗并关启动窗，关闭项目回到启动窗；OS 双击 `.kiw` 直达编辑窗。
- editor 新增「启用外部控制」（设置 → AI，默认关）：开启后本地起一个只绑 `127.0.0.1`、带 token 鉴权的接口，外部 agent（如本地 Claude Code）可经新的 `kiny` 命令行驱动运行中的 editor 创作 Kin 故事——能力等同内置 AI，改动同样落编辑器脏缓冲、作者实时可见可撤销；主界面常驻「外部控制已启用 · 端口 N」指示，随时可关。附 `kiny-authoring` skill 教外部 agent 上手。

### Changed

- editor 预览也支持 `@input`：预览推进到输入命令时输入框可直接填写提交（此前仅阅读器可交互，预览只显示禁用占位），提交后逐段揭示后续正文；AI 写作助手也能在预览里驱动输入框。

### Fixed

- editor 设置弹窗不再因误点遮罩而关闭，弹窗尺寸调大；窗口尺寸记忆遇最小化的 0×0 不再令下次启动工作台塌陷。

## [0.6.0] - 2026-07-07
### Added

- editor 冷启动改为先展示启动页（新建项目 / 打开项目 / 最近项目入口），点击后进入编辑器；文件菜单「最近打开」列出近期项目、并可「关闭项目」回到启动页。
- editor 新增独立「项目设置」弹窗（文件菜单 → 项目设置…）：可修改项目名称（连带把项目 `.kiw` 文件重命名）、启动入口 `.kin`、项目版本号。
- Kin 新增 `@clear` 清屏指令：清除屏幕上已显示的正文文字，保留背景图与背景音乐；player / reader / editor 预览一并生效。

### Changed

- editor 设置弹窗重构为多 tab 页签（排版 / 外观 / 编辑器 / AI）：左侧分类导航 + 右侧对应页，减少滚动、分类清晰，各 tab 有未保存改动时导航上显示脏点。

### Fixed

- 修复 editor 隐藏预览 / 侧栏 / AI 面板后编辑区没撑满、右侧留白或面板错位的问题。

## [0.5.0] - 2026-07-02
### Added

- editor 视图菜单新增「保存视图 / 恢复我的布局 / 恢复默认布局」：可把当前面板布局存成快照，一键回到保存点或出厂布局（未保存过时不显示「恢复我的布局」）。
- editor 预览新增「随机种子」开关：开启后每次重开预览（↺）重掷种子，方便观察含随机 / shuffle 的分支多样性；关闭则保持确定性、结果可复现，种子指示器随模式显示。
- editor 资源面板右键新增「导入资源…」：对文件夹 / 文件 / 根节点右键即可选系统图片、音频导入到该目录；重名可选覆盖 / 改名 / 跳过，并能「应用到其余」批量处理。
- editor 打开项目改为选 `.kiw` 项目文件，并支持在系统里双击 `.kiw` 直接用 editor 打开；新建项目产出 `.kiw`，旧的 `kiny.json` 项目打开时自动迁移；可打开任意盘符下的项目。
- Windows 打包新增免安装 portable 版：editor / reader 各出一份解压即用的 zip（不写注册表、不需安装），与安装包一起放进发布列表；缺 WebView2 运行时会弹提示引导下载。
- 新增 Android 版阅读器：手机上可导入 `.kip` 并阅读，支持从微信 / QQ / 文件管理器等「分享」或「打开方式」一点直达 reader；适配移动端窄屏（书架单列、按钮触摸尺寸、安全区避让）。
- editor AI 对话历史现可持久化：对话随 app 重启留存，按项目归属；支持多会话（新建 / 切换 / 删除），并可设置「超过 N 天无新增自动清理」（默认 30 天，可关）；对话仅存本机，含 key 的请求不入库。

### Changed

- editor 资源管理器取消拖动移动，改为纯点击的「移动到…」（右键选目标目录或根目录完成移动），触摸设备更好用；创建 / 重命名 / 新建不受影响。
- editor 新建项目不再默认创建空 `assets` 文件夹（首次导入资源时按需创建）。

### Fixed

- 修复 editor 预览区竖直滚动条未贴面板右缘、悬在中间的问题（阅读列仍居中）。

## [0.4.0] - 2026-06-28
### Added

- editor 接入 AI：配置自己的 LLM（OpenAI 兼容：OpenAI / DeepSeek / GLM / Ollama 等），在 AI 面板里让 AI 帮你建 / 改节点，改动落进可撤销的编辑缓冲；key 仅存本机、直连不中转。
- editor 内置 AI 现可查阅 Kin 语言规范：AI 拿不准语法时按章节查规则 / 示例 / 边界（以语言规范为唯一来源），写出的 Kin 更准。
- editor 自动保存与崩溃恢复：未保存改动会在后台写入恢复草稿（不碰真文件），崩溃或强制退出后重开项目可一键恢复；设置里可开关「自动恢复草稿」（默认开）。

### Fixed

- 修复遇到「无玩家交互的死循环」（如漏写选项标记致无限自动跳转）时界面完全卡死：现改为报错并指出出错节点，editor / reader 正常显示错误而非冻结。同时 editor 编辑期对「漏写 `*` / `+` 选项标记、被当成正文+跳转」的写法给出告警。

## [0.3.0] - 2026-06-26
### Added

- reader 支持存档读档：自动续读（读一半重开从断点继续）+ 手动多存档（存 / 读 / 删，删除需二次确认、存档有「已存档」提示）。

### Changed

- editor 编辑内核迁移到 CodeMirror 6：查找 / 替换、多光标、代码折叠、括号匹配、行内诊断波浪线，以及节点名 / 变量 / @命令自动补全与节点跳转（F12 / Ctrl+点击）。

## [0.2.0] - 2026-06-24
### Added

- 文本支持内联富文本：粗体 / 斜体 / 下划线 / 删除线、颜色、字号与换行。
- editor 跟进内联富文本：语法参考新增「内联富文本」一节，编辑器高亮识别富文本标签。
- editor / reader 运行时错误收集与问题反馈：错误落本地日志、崩溃有兜底页，帮助菜单可一键反馈。
- editor 可把项目导出成自包含独立网页：file:// 双击即读完整故事（含图片 / 音频），不依赖联网。

### Changed

- editor 设置弹窗字号 / 行距步进器视觉优化：px 单位独立分格、数字不再跳动。

## [0.1.0] - 2026-06-21
### Added

- 实现 Kiny DSL 引擎:parser(文本→AST)、analyze(跨文件静态检查)、runtime(状态机执行)与 CLI 终端播放器。
- 支持完整叙事语法:节点/子节点、选项(一次性/粘性/条件/fallback)、`>` 分支汇合、`<>` 粘连、文本变体(`seq`/`cycle`/`once`/`shuffle`)、`@if` 条件块、内联 JS 求值、带参节点、回合数与可复现随机。
- 提供平台无关的 React 播放层 player:受控 `<Player>` 组件、driver/host 抽象、确定性保位重放。
- 支持背景、音频与一次性音效(`@sfx`)命令。
- 实现故事状态快照:`Story.serialize` / `restoreStory` 往返等价。
- 新增浏览器端故事阅读器 web-reader(Vite + React)。
- 新增 Tauri 2 桌面编辑器 editor:多文件项目、文件树增删改/拖拽、跨文件实时校验、保位预览、多 tab 与会话恢复、语义着色、排版与主题设置、`.kip` 故事包导出、未保存守卫。
- 新增 Tauri 2 桌面阅读器 reader:`.kip` 导入(按钮/拖放)、持久书架与阅读屏。
- 内置样例项目《雾港之夜》。
